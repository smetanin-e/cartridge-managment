# 🖨️ Система управления картриджами

Корпоративное веб-приложение для автоматизации учёта, обслуживания и контроля жизненного цикла картриджей печатающей техники предприятия.

Проект решает задачу прозрачного управления картриджами: от регистрации и установки в принтер до отправки в сервисный центр и возврата после заправки.

---

## 🎯 Цель проекта

Автоматизировать процессы, которые ранее выполнялись вручную:

- учёт картриджей и их состояний;
- замена картриджей в подразделениях;
- формирование партий для сервисного центра;
- частичный и полный возврат картриджей;
- контроль доступности и резервов.

Проект ориентирован на использование внутри организации и учитывает реальные бизнес-сценарии эксплуатации оргтехники.

---

## 🧰 Технологический стек

### Frontend

- **Next.js 15 (App Router)**
- **React 19**
- **TypeScript 5**
- Tailwind CSS 4
- Radix UI, Lucide Icons
- TanStack Query 5
- React Hook Form
- Zod
- Zustand

### Backend / Data

- Next.js API Routes (GET only)
- **Server Actions** (мутации)
- **Prisma ORM 6**
- **PostgreSQL**
- Axios

### Авторизация и UX

- NextAuth
- Role-Based Access Control (USER / ADMIN)
- React Hot Toast
- Next Themes

---

## 🧱 Архитектура проекта

Проект построен на разделении **чтения и изменения данных**:

### 📡 Получение данных

- Все запросы на чтение выполняются через **API маршруты (GET)**.
- API используется только для получения данных.
- Все запросы защищены проверкой авторизации.

### ⚡ Изменение данных

- Создание, обновление и удаление данных выполняются **через Server Actions**.
- Server Actions напрямую работают с базой данных через Prisma.
- Вызов серверных экшенов на клиенте осуществляется через **TanStack Query мутации**.

### 🔄 Синхронизация состояния

- TanStack Query отвечает за:
  - кэширование данных;
  - автоматические refetch’и;
  - инвалидацию кэша после мутаций;
  - реактивное обновление интерфейса.

---

## 🔐 Пользователи и роли

В системе реализована ролевая модель доступа:

- **USER**
  - работа с заменами картриджей;
  - формирование партий;
  - просмотр реестров и статистики.

- **ADMIN**
  - управление пользователями;
  - назначение ролей;
  - блокировка и сброс паролей.

Самостоятельная регистрация пользователей отключена.  
Все учетные записи создаются администратором.

---

## 🔄 Жизненный цикл картриджей

Каждый картридж имеет уникальный инвентарный номер и может находиться в одном из состояний:

- `AVAILABLE` — готов к использованию
- `WORKING` — установлен в принтер
- `RESERVE` — находится в резерве
- `SERVICE` — отправлен в сервисный центр
- `REFILL` — требуется заправка

Состояния автоматически изменяются при выполнении операций (замена, отправка, возврат).

---

## 📦 Основной функционал

### 🔹 Реестр картриджей

- регистрация картриджей;
- фильтрация по моделям и состояниям;
- просмотр текущего статуса и истории.

### 🔹 Учёт принтеров

- регистрация моделей принтеров;
- привязка совместимых моделей картриджей;
- определение совместимости оборудования.

### 🔹 Замена картриджей

- фиксация замен в подразделениях;
- указание снятого и установленного картриджа;
- ведение истории операций.

### 🔹 Работа с партиями

- формирование партий для отправки в сервис;
- отслеживание партий в работе;
- частичный и полный возврат картриджей;
- архив завершённых партий.

### 🔹 Администрирование

- управление пользователями;
- смена ролей;
- блокировка и сброс паролей.

---

## 📝 Формы и валидация

- Все формы реализованы с использованием **React Hook Form**.
- Для валидации данных применяется **Zod**.
- Ошибки отображаются пользователю до отправки данных.
- После успешных операций выполняется автоматическая инвалидация кэша.

---

## 🗂 Структура проекта

Проект организован по **feature-based архитектуре**, что упрощает поддержку и масштабирование:
